import React from 'react';
import Router from './Router/Router';
import Layout from './layout/Layout';

const App = () => {
  return (
    <Layout>
      <Router />
    </Layout>
  );
};

export default App;
